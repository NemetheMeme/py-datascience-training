import numpy as np

#### array with array
#just use the operators with raw arrays
# array + array
#### arrays with Scalars
# the same as array with array => array + 100 (the scalar)
#### universal array functions